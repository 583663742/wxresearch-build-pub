#!/bin/bash
# postrm - ???????????? + ????
# ?? ellekit ?????????????

set -e

# ??????
rm -f /var/mobile/wxresearch_ctor.txt 2>/dev/null || true
rm -f /tmp/wxresearch_ctor.txt 2>/dev/null || true

# ??????????????????
WX_DOC=$(find /var/mobile/Containers/Data/Application -name "wxresearch.log" -path "*/Documents/*" 2>/dev/null | head -5)
for f in $WX_DOC; do
    rm -f "$f" 2>/dev/null || true
done

# ?? NSUserDefaults ??????
# (????????? plist???????????)

# ???? UI ????? uicache ???
if command -v uicache >/dev/null 2>&1; then
    uicache -p com.tencent.xin 2>/dev/null || true
fi

exit 0
